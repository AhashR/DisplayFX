<UserControl x:Class="DisplayFX.Interface.Profiles.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:profiles="clr-namespace:DisplayFX.Interface.Profiles"
             mc:Ignorable="d"
             Margin="0,0,0,6"
             ContextMenu="{Binding ContextMenu}"
             d:DesignHeight="60" d:DesignWidth="220" d:DataContext="{d:DesignInstance profiles:ProfileViewModel}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Button
        Style="{DynamicResource ProfileCardButtonStyle}"
        Click="ProfileCard_Click"
        HorizontalContentAlignment="Stretch">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Name + Icon -->
            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock
                    Text="{Binding Name}"
                    FontSize="13"
                    FontWeight="SemiBold"
                    TextTrimming="CharacterEllipsis"
                    VerticalAlignment="Center" />
            </StackPanel>

            <!-- Active Star Indicator -->
            <Path
                Grid.Row="0"
                Grid.Column="1"
                Width="12"
                Height="12"
                Stretch="Uniform"
                Fill="{DynamicResource AccentBlueBrush}"
                Data="M 12 2 L 15.09 8.26 L 22 9.27 L 17 14.14 L 18.18 21.02 L 12 17.77 L 5.82 21.02 L 7 14.14 L 2 9.27 L 8.91 8.26 Z"
                Visibility="{Binding IsActive, Converter={StaticResource BooleanToVisibilityConverter}}"
                ToolTip="Active Profile"
                VerticalAlignment="Center"
                HorizontalAlignment="Right" />

            <!-- Target App Subtitle -->
            <TextBlock
                Grid.Row="1"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Text="{Binding Profile.LinkedExecutablePath}"
                FontSize="11"
                Foreground="{DynamicResource TextMutedBrush}"
                TextTrimming="CharacterEllipsis"
                Margin="0,2,0,0" />
        </Grid>
    </Button>
</UserControl>